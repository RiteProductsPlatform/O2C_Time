# Exclude Oracle Visual Builder Files

node_modules/
build/
private/

# Python (integration/ helper scripts)
__pycache__/
*.py[cod]
.venv/

# BIP extract output — contains real worker names and email addresses
integration/bip/extracts/
integration/bip/tmp/

# Local-only, never pushed
# .mcp.json carries a plaintext MCP API key.
# CLAUDE.md is local operating notes, not a project deliverable.
.mcp.json
CLAUDE.md

# Fusion credentials for the BIP loader. The .example is committed, the real
# one never is.
integration/bip/.env

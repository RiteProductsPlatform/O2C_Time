define([], () => {
  'use strict';

  /**
   * PAGE-007 Salary Stopping — payroll holds arising from defaulted timesheets
   * (PROC-007). Visible to managers for their own team and to admins for the
   * whole period.
   */
  class SalaryStoppingFlowModule {
  }

  return SalaryStoppingFlowModule;
});

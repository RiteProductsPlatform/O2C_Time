define([], () => {
  'use strict';

  /**
   * PAGE-005 Approval Detail — the deepest level of the manager drill-down
   * (PER-003). Project / period / employee / week context is at application
   * scope so Back returns to the summary with the selection intact.
   */
  class ApprovalDetailFlowModule {
  }

  return ApprovalDetailFlowModule;
});
